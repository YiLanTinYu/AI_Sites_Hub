# 待线上验证

本文记录当前只能在本地验证、尚未完成线上验证的项目。后续条件成熟后，应按顺序逐项执行并记录结果。

## 当前阻塞条件

- [x] 当前阶段代码已提交、推送并合并到 GitHub 默认分支（PR #1，合并提交 `4f63090`）。
- [x] 本机已安装 GitHub CLI `gh`（2.95.0）。
- [x] GitHub CLI 已授权账号 `YiLanTinYu`。
- [x] GitHub 仓库已识别并启用 `Deal monitor` 工作流。

## 一、准备 GitHub CLI

- [x] 安装 GitHub CLI。

```bash
brew install gh
```

- [x] 登录 GitHub。

```bash
gh auth login
gh auth status
```

预期结果：`gh auth status` 显示已登录 GitHub，并且账号对
`YiLanTinYu/AI_Sites_Hub` 仓库具有推送和 Actions 操作权限。

## 二、提交并推送当前代码

- [x] 已确认工作区改动全部属于 AI Sites Hub 项目。
- [x] 已再次执行生产构建：32 个文件 0 错误、0 警告，成功生成 63 个页面。
- [x] 已再次执行优惠监测：变化 0、临近结束 1、已到期 0、异常 0。
- [x] Git 差异格式检查通过。
- [x] 已提交当前阶段代码（提交 `be890d0`）。
- [x] 已推送到 GitHub 分支 `codex/online-validation`。

```bash
cd /Users/zengjun/Desktop/sites_hub
npm run build
npm run discover:deals
git diff --check
git status --short
```

推送后确认 GitHub 仓库中包含：

- [x] `.github/workflows/deal-monitor.yml`
- [x] `src/data/deals.json`
- [x] `src/data/deal-sources.json`
- [x] `tools/discover-deals.mjs`
- [x] `reports/deal-snapshots.json`

## 三、确认工作流已被 GitHub 识别

- [x] 已列出仓库工作流，`Deal monitor` 与 `Exchange rate monitor` 均为 `active`。

```bash
gh workflow list --repo YiLanTinYu/AI_Sites_Hub
```

预期结果：列表中出现 `Deal monitor`，状态为启用。

- [x] 已在 GitHub 网页打开仓库的 `Actions` 页面。
- [x] 已确认 `Deal monitor` 位于工作流列表。
- [x] 已确认仓库没有禁用 GitHub Actions（两条工作流状态均为 `active`）。

## 四、手动触发首次线上巡检

- [x] 已手动触发工作流（Run ID `28523871899`）。

```bash
gh workflow run "Deal monitor" --repo YiLanTinYu/AI_Sites_Hub
```

- [x] 已查询最近一次运行，Run ID 为 `28523871899`，触发方式为 `workflow_dispatch`。

```bash
gh run list \
  --repo YiLanTinYu/AI_Sites_Hub \
  --workflow "Deal monitor" \
  --limit 5
```

- [x] 已等待 Run `28524043289` 结束并查看完整日志。

```bash
gh run watch RUN_ID --repo YiLanTinYu/AI_Sites_Hub
gh run view RUN_ID --repo YiLanTinYu/AI_Sites_Hub --log
```

预期结果：以下步骤全部成功。

- [x] Checkout repository
- [x] Set up Node.js
- [x] Install dependencies
- [x] Validate and build
- [x] Monitor official deal pages
- [x] Upload review report

## 五、验证线上巡检结果

- [x] 工作流最终状态为 `success`（Run `28524043289`，耗时 31 秒）。
- [x] 构建结果为 0 errors、0 warnings。
- [x] 已确认单个网站访问失败不会中断整个工作流（测试 Run `28551064224`）。
- [x] 运行摘要能看到页面变化、临近结束、已到期和访问异常数量。
- [x] Actions 运行页面生成 `ai-deal-report-28524043289` 附件。
- [x] 已下载并解压附件到临时目录核验。
- [x] 附件中包含当日优惠监测报告 `ai-deals-2026-07-01.md`。
- [x] 附件中包含 `deal-snapshots.json`。
- [x] 报告正确提示 Kimi 活动于 2026-07-02 临近结束。
- [x] 报告未把稳定页面误报为发生变化（页面变化为 0）。

## 六、验证每日定时执行

工作流计划时间为：

```yaml
cron: "17 1 * * *"
```

对应北京时间每天 09:17 左右。GitHub Actions 定时任务可能存在数分钟延迟。

- [ ] 至少等待一次计划执行。
- [ ] 确认触发方式为 `schedule`，而不是 `workflow_dispatch`。
- [ ] 确认计划运行状态为 `success`。
- [ ] 确认计划运行同样生成审核报告附件。
- [ ] 连续观察至少 3 天，确认工作流能稳定执行。
- [ ] 记录三次运行的 Run ID、开始时间、结束时间和结果。

| 日期 | Run ID | 触发方式 | 结果 | 报告异常 |
| --- | --- | --- | --- | --- |
| 待验证 |  |  |  |  |
| 待验证 |  |  |  |  |
| 待验证 |  |  |  |  |

## 七、模拟页面变化

此项用于验证“页面变化报告”链路，不应修改线上优惠内容。

- [x] 已在测试分支中临时修改 Kimi 监测源关键词。
- [x] 已推送测试分支，并从该分支手动运行工作流。
- [x] 已触发测试巡检（Run `28551064224`）。
- [x] 报告将 Kimi 列入“需要人工复核”。
- [x] 已确认脚本没有自动修改 `src/data/deals.json`。
- [x] 已删除本地与远程临时测试分支及测试改动。

## 八、模拟优惠过期

- [x] 已在测试分支复制一条测试优惠。
- [x] 已将测试优惠的 `endsAt` 设置为过去日期。
- [x] 已执行线上工作流（Run `28552595350`）。
- [x] 监测报告将测试记录列入“已到期”。
- [x] 已构建测试分支预览页面，0 errors、0 warnings。
- [x] 优惠专区将测试记录放入“历史优惠”。
- [x] 首页未展示该测试优惠。
- [x] 已删除测试记录及分支，测试数据未合并到 `main`。

## 九、验证报告留存

- [x] 已确认 Actions 附件保留时间为 30 天（截至 2026-07-31）。
- [x] 已确认报告可在保留期内下载并解压。
- [x] 已确认报告中不包含账号、Cookie、API Key 等敏感信息。
- [x] 已评估自动创建 Issue/PR：当前保留 Actions 摘要与附件，避免产生每日噪声。
- [x] 当前未启用报告 Issue/PR；如后续启用，仍必须人工审核后发布。

## 十、线上部署联动

在网站部署流程接入 GitHub 后验证：

- [ ] 工作流失败时不会部署未经验证的数据。
- [x] Cloudflare Pages 已连接 `main`，首次自动构建与部署成功。
- [x] 线上 `/deals/` 页面可以访问。
- [x] 线上费用计算器可以显示对应模型的优惠提示。
- [x] 线上“查看官方活动”链接均指向去除跟踪参数后的官方地址。
- [ ] 已结束优惠不会继续出现在首页。

## 十一、Phase 3 自动化

- [x] GitHub Actions 中已出现并启用 `Exchange rate monitor`。
- [x] 已手动触发汇率更新工作流（Run ID `28524337632`）。
- [x] 已确认工作流读取欧洲央行官方数据（2026-07-01：1 USD = 6.7945 CNY）。
- [x] 已确认汇率变化时创建 `automation/exchange-rate` 分支和待审核 PR #4。
- [x] 已确认汇率未变化时不会创建空提交或 PR（Run `28524702028`）。
- [x] 已审核并合并汇率更新 PR #4，仅修改 `src/data/exchange-rates.json`。
- [x] 线上费用计算器使用最新参考汇率 `1 USD = 6.7945 CNY`。
- [x] 模型详情页价格历史图在生产环境生成有效画布。
- [x] 价格更新记录页 `/updates/` 返回 HTTP 200。
- [x] 场景推荐页 `/recommendations/` 返回 HTTP 200。
- [x] 已提交并关闭测试 Issue #6，确认“价格或优惠失效”反馈入口有效。

## 验证完成标准

满足以下条件后，才可认为 GitHub Actions 每日优惠巡检已经通过线上验证：

1. 手动触发成功至少 1 次。
2. 定时触发连续成功至少 3 次。
3. 审核报告和快照附件均可下载。
4. 页面变化、临近结束和过期状态均经过测试。
5. 工作流不会自动发布未经人工核实的优惠。
6. 日志和报告不包含敏感信息。

## 十二、Phase 4 用户服务

- [x] 线上 `/watchlist/` 可以添加、移除关注模型。
- [x] 刷新页面后本地关注和目标价格仍然保留。
- [ ] 不同浏览器之间不会错误共享本地关注数据。
- [x] `/feeds/deals.json` 返回有效 JSON 和当前优惠。
- [x] `/feeds/updates.json` 返回有效 JSON 和价格历史。
- [x] `/feeds/deals.xml` 返回有效 Atom Feed 与条目。
- [x] `/feeds/updates.xml` 返回有效 Atom Feed 与条目。
- [x] `/submit/` 的三类投稿链接能正确预填 GitHub Issue。
- [x] 投稿入口只创建待人工审核 Issue，不会自动修改或发布网站数据。
- [ ] 在 GitHub Actions Secrets 中配置测试用 `DEAL_WEBHOOK_URL`。
- [ ] 手动触发 `Deal monitor`，确认有事件时 Webhook 收到 JSON。
- [ ] 确认无变化、无临期、无异常时不会发送空通知。
- [ ] 确认 Actions 日志不会显示 Webhook 地址或其他敏感信息。
- [ ] 删除测试 Secret 或替换为正式 Webhook。

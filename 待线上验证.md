# 待线上验证

本文记录当前只能在本地验证、尚未完成线上验证的项目。后续条件成熟后，应按顺序逐项执行并记录结果。

## 当前阻塞条件

- [ ] 当前 Phase 1、Phase 2 代码尚未提交并推送到 GitHub。
- [x] 本机已安装 GitHub CLI `gh`（2.95.0）。
- [x] GitHub CLI 已授权账号 `YiLanTinYu`。
- [ ] GitHub 仓库中尚未出现 `Deal monitor` 工作流。

## 一、准备 GitHub CLI

- [x] 安装 GitHub CLI。

```bash
brew install gh
```

- [x] 登录 GitHub。

```bash
gh auth login
gh auth status
```

预期结果：`gh auth status` 显示已登录 GitHub，并且账号对
`YiLanTinYu/AI_Sites_Hub` 仓库具有推送和 Actions 操作权限。

## 二、提交并推送当前代码

- [x] 已确认工作区改动全部属于 AI Sites Hub 项目。
- [x] 已再次执行生产构建：32 个文件 0 错误、0 警告，成功生成 63 个页面。
- [x] 已再次执行优惠监测：变化 0、临近结束 1、已到期 0、异常 0。
- [x] Git 差异格式检查通过。
- [ ] 提交当前 Phase 1、Phase 2 代码。
- [ ] 推送到 GitHub。

```bash
cd /Users/zengjun/Desktop/sites_hub
npm run build
npm run discover:deals
git diff --check
git status --short
```

推送后确认 GitHub 仓库中包含：

- [ ] `.github/workflows/deal-monitor.yml`
- [ ] `src/data/deals.json`
- [ ] `src/data/deal-sources.json`
- [ ] `tools/discover-deals.mjs`
- [ ] `reports/deal-snapshots.json`

## 三、确认工作流已被 GitHub 识别

- [ ] 列出仓库工作流。

```bash
gh workflow list --repo YiLanTinYu/AI_Sites_Hub
```

预期结果：列表中出现 `Deal monitor`，状态为启用。

- [ ] 在 GitHub 网页打开仓库的 `Actions` 页面。
- [ ] 确认 `Deal monitor` 位于左侧工作流列表。
- [ ] 确认仓库没有禁用 GitHub Actions。

## 四、手动触发首次线上巡检

- [ ] 手动触发工作流。

```bash
gh workflow run "Deal monitor" --repo YiLanTinYu/AI_Sites_Hub
```

- [ ] 查询最近一次运行。

```bash
gh run list \
  --repo YiLanTinYu/AI_Sites_Hub \
  --workflow "Deal monitor" \
  --limit 5
```

- [ ] 等待运行结束并查看详细日志。

```bash
gh run watch RUN_ID --repo YiLanTinYu/AI_Sites_Hub
gh run view RUN_ID --repo YiLanTinYu/AI_Sites_Hub --log
```

预期结果：以下步骤全部成功。

- [ ] Checkout repository
- [ ] Set up Node.js
- [ ] Install dependencies
- [ ] Validate and build
- [ ] Monitor official deal pages
- [ ] Upload review report

## 五、验证线上巡检结果

- [ ] 工作流最终状态为 `success`。
- [ ] 构建结果为 0 errors、0 warnings。
- [ ] 优惠监测没有因单个网站访问失败而中断整个工作流。
- [ ] 运行摘要能看到页面变化、临近结束、已到期和访问异常数量。
- [ ] Actions 运行页面生成 `ai-deal-report-RUN_ID` 附件。
- [ ] 下载并解压附件。
- [ ] 附件中包含当日优惠监测报告。
- [ ] 附件中包含 `deal-snapshots.json`。
- [ ] 报告能够正确提示 Kimi 活动临近结束。
- [ ] 报告不会把稳定页面误报为发生变化。

## 六、验证每日定时执行

工作流计划时间为：

```yaml
cron: "17 1 * * *"
```

对应北京时间每天 09:17 左右。GitHub Actions 定时任务可能存在数分钟延迟。

- [ ] 至少等待一次计划执行。
- [ ] 确认触发方式为 `schedule`，而不是 `workflow_dispatch`。
- [ ] 确认计划运行状态为 `success`。
- [ ] 确认计划运行同样生成审核报告附件。
- [ ] 连续观察至少 3 天，确认工作流能稳定执行。
- [ ] 记录三次运行的 Run ID、开始时间、结束时间和结果。

| 日期 | Run ID | 触发方式 | 结果 | 报告异常 |
| --- | --- | --- | --- | --- |
| 待验证 |  |  |  |  |
| 待验证 |  |  |  |  |
| 待验证 |  |  |  |  |

## 七、模拟页面变化

此项用于验证“页面变化报告”链路，不应修改线上优惠内容。

- [ ] 在测试分支中临时修改一个监测源的关键词。
- [ ] 推送测试分支，并临时允许工作流从该分支手动运行。
- [ ] 触发一次巡检。
- [ ] 确认报告将对应来源列入“需要人工复核”。
- [ ] 确认脚本没有自动修改 `src/data/deals.json`。
- [ ] 删除临时测试改动。

## 八、模拟优惠过期

- [ ] 在测试分支复制一条测试优惠。
- [ ] 将测试优惠的 `endsAt` 设置为过去日期。
- [ ] 执行线上工作流。
- [ ] 确认监测报告将其列入“已到期”。
- [ ] 构建测试分支预览页面。
- [ ] 确认优惠专区把该记录放入“历史优惠”。
- [ ] 确认首页不再展示该优惠。
- [ ] 删除测试记录，不合并测试数据。

## 九、验证报告留存

- [ ] 确认 Actions 附件保留时间为 30 天。
- [ ] 确认旧报告可以在保留期内下载。
- [ ] 确认报告中不包含账号、Cookie、API Key 等敏感信息。
- [ ] 评估是否需要将审核报告自动提交为 GitHub Issue 或 Pull Request。
- [ ] 若启用 Issue 或 Pull Request，必须继续遵守“人工审核后发布”原则。

## 十、线上部署联动

在网站部署流程接入 GitHub 后验证：

- [ ] 工作流失败时不会部署未经验证的数据。
- [ ] 合并到 `main` 后能触发网站构建和部署。
- [ ] 线上 `/deals/` 页面可以访问。
- [ ] 线上费用计算器可以显示对应模型的优惠提示。
- [ ] 线上“查看官方活动”链接指向去除跟踪参数后的官方地址。
- [ ] 已结束优惠不会继续出现在首页。

## 十一、Phase 3 自动化

- [ ] GitHub Actions 中出现 `Exchange rate monitor`。
- [ ] 手动触发汇率更新工作流。
- [ ] 确认工作流读取欧洲央行官方数据。
- [ ] 确认汇率变化时创建 `automation/exchange-rate` 分支和待审核 Pull Request。
- [ ] 确认汇率未变化时不会创建空提交。
- [ ] 审核并合并一次汇率更新 Pull Request。
- [ ] 确认线上费用计算器使用合并后的最新参考汇率。
- [ ] 确认模型详情页价格历史图在生产环境正常渲染。
- [ ] 确认价格更新记录页 `/updates/` 可以访问。
- [ ] 确认场景推荐页 `/recommendations/` 可以访问。
- [ ] 提交一次“价格或优惠失效”测试 Issue，确认反馈入口有效。

## 验证完成标准

满足以下条件后，才可认为 GitHub Actions 每日优惠巡检已经通过线上验证：

1. 手动触发成功至少 1 次。
2. 定时触发连续成功至少 3 次。
3. 审核报告和快照附件均可下载。
4. 页面变化、临近结束和过期状态均经过测试。
5. 工作流不会自动发布未经人工核实的优惠。
6. 日志和报告不包含敏感信息。

## 十二、Phase 4 用户服务

- [ ] 线上 `/watchlist/` 可以添加、移除关注模型。
- [ ] 刷新页面后本地关注和价格阈值仍然保留。
- [ ] 不同浏览器之间不会错误共享本地关注数据。
- [ ] `/feeds/deals.json` 返回有效 JSON 和当前优惠。
- [ ] `/feeds/updates.json` 返回有效 JSON 和价格历史。
- [ ] `/feeds/deals.xml` 能被 Atom 阅读器订阅。
- [ ] `/feeds/updates.xml` 能被 Atom 阅读器订阅。
- [ ] `/submit/` 的三类投稿链接能正确预填 GitHub Issue。
- [ ] 确认投稿 Issue 不会自动修改或发布网站数据。
- [ ] 在 GitHub Actions Secrets 中配置测试用 `DEAL_WEBHOOK_URL`。
- [ ] 手动触发 `Deal monitor`，确认有事件时 Webhook 收到 JSON。
- [ ] 确认无变化、无临期、无异常时不会发送空通知。
- [ ] 确认 Actions 日志不会显示 Webhook 地址或其他敏感信息。
- [ ] 删除测试 Secret 或替换为正式 Webhook。
